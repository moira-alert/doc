get_patterns:
  200:
    description: "Patterns fetched successfully"
    content:
      applicaton/json:
        schema:
          type: object
          properties:
            list:
              type: array
              description: "List of fetched patterns"
              items:
                $ref: "../schemas/Pattern.yml#/Pattern"

delete_pattern:
  200:
    description: "Pattern delete successfully"
