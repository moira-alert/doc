user:
  get:
    summary: "Gets the username of the authenticated user if it is available."
    operationId: GetUser
    tags:
      - user
    responses:
      $ref: ./responses.yml#/get

settings:
  get:
    summary: "Get the user's contacts and subscriptions."
    operationId: GetUserSettings
    tags:
      - user
    responses:
      $ref: ./responses.yml#/get_settings